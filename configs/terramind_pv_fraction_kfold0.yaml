# TerraTorch fine-tuning config for PV FRACTION regression -- k-fold 0,
# 6 quadrats held out (nasirabad_rural, sanghar, sundar, rahim_yar_khan, peshawar, lahore). Generated by
# scripts/build_fraction_kfold.py, 2026-08-14 -- see that script's docstring for the
# fold assignment and the two leakage checks. Same recipe as
# configs/terramind_pv_fraction_pakistan_quadrats_holdout_karachi.yaml in every
# respect except the corpus (backbone, necks, decoder, loss, lr, schedule).
task_type: regression

data:
  index_path: data/chips/combined_fraction_kfold/fold0/index.parquet
  batch_size: 8
  num_workers: 4
  target: fraction

task:
  model_factory: EncoderDecoderFactory
  model_args:
    backbone: terramind_v1_tiny
    backbone_pretrained: true
    backbone_modalities: [S2L2A]
    backbone_bands:
      S2L2A: [BLUE, GREEN, RED, RED_EDGE_1, RED_EDGE_2, RED_EDGE_3,
              NIR_BROAD, NIR_NARROW, SWIR_1, SWIR_2]
    necks:
      - name: SelectIndices
        indices: [2, 5, 8, 11]
      - name: ReshapeTokensToImage
        remove_cls_token: false
      - name: LearnedInterpolateToPyramidal
    decoder: UNetDecoder
    decoder_channels: [256, 128, 64, 32]
    head_final_act: torch.nn.Sigmoid
    rescale: true
  loss: weighted_mse
  weighted_mse_args: {k: 10.0}
  ignore_index: -1
  lr: 1.0e-4
  optimizer: AdamW
  optimizer_hparams:
    weight_decay: 0.05
  freeze_backbone: false
  plot_on_val: false

trainer:
  max_epochs: 60
  accumulate_grad_batches: 2

checkpoint_dir: data/models/fraction_kfold0
patience: 8
monitor: val/RMSE
monitor_mode: min
