# OSM `power=plant` ground perimeters in Zambia that contain NO built array.
#
# Screened by scripts/screen_osm_ground_perimeters.py (v4_combined_all epoch=41, the
# 2025-05-01..2025-10-15 dry-season composite) and then HAND-AUDITED against the RGB
# imagery one by one -- the screen only ranks what to look at, the verdict came from
# looking. Each of these is fully imaged (imaged_frac 0.667-1.000) and returns p_max of
# exactly 0.00: not a low mean, no pixel anywhere inside it that the model finds even
# slightly PV-like. The same checkpoint covers Bangweulu at 98.8% and Itimpi at 99.7% of
# their imaged area in the same country and epoch, so it is not blind to Zambian
# ground-mount.
#
# What the imagery shows, polygon by polygon:
#   1547172667  1.80 km2  graded, cleared bare ground, no panel rows
#   1547702741  1.52 km2  bush and scrub with a track and a few buildings
#   1547167645  1.16 km2  farmland and scrub with scattered trees
#   1547170720  1.15 km2  woodland and scrub
#   1547146887  0.74 km2  cultivated fields
#   1442146965  0.29 km2  bare cleared ground (Copperbelt); 0.667 imaged, the least certain
#
# Together 6.67 km2, which converts to 333.6 MWp at DEFAULT_KWP_PER_M2_LAND -- booked into
# the evidence atlas's Verified tier, and burned as positive training masks by `chips`,
# if they are left in. Five of the six were mapped in one pass on 2026-08-04; they look
# like announced or permitted project sites, which OSM does tag, rather than mismapping.
#
# THIS IS NOT A CLAIM THAT THE PROJECTS DO NOT EXIST -- only that no array was built as of
# the imagery epoch. Re-screen after new imagery; a site that fills in should come back.
#
# 2026-09-15, after the national compose finished (1,843/1,843 cells): the three
# previously-unjudged perimeters were re-screened over full coverage.
#   1547162867  2.76 km2  imaged 1.000, p_max 0.00 -> imagery shows uniform bare reddish
#                         ground with an eroded rocky edge. EMPTY, added below.
#   1547146891  2.23 km2  imaged 1.000, p_max 0.00 -> imagery shows mottled bush and
#                         scrub woodland. EMPTY, added below.
#   1510572941  2.08 km2  STILL UNJUDGEABLE, and not for lack of compose: its cell
#                         (0069_0068) has a genuine nodata hole exactly under the polygon
#                         -- 25,993 pixels, zero in all ten bands, well inside the raster
#                         bounds -- and the cell is only 22.8% filled overall. That is a
#                         rare defect (of 120 cells sampled nationally, median fill is
#                         100% and only 2 are below 50%), not a systemic one. It stays IN
#                         the atlas, contributing ~104 MWp at the land constant. To settle
#                         it, re-compose 0069_0068 on a wider window or via the other STAC
#                         provider, then re-run the screen.
osm-way/1547172667
osm-way/1547702741
osm-way/1547167645
osm-way/1547170720
osm-way/1547146887
osm-way/1442146965
osm-way/1547162867
osm-way/1547146891
